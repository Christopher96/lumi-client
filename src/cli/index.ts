import { InteractiveCLI } from './interactive';

export class CLI {
  constructor() {
    // TODO Create a CLI with options instead of interactive

    // --interactive
    new InteractiveCLI();
  }
}
